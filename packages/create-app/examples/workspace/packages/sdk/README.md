# @example/todo.sdk

Generated, typed SDK for Example todo application.

**This package is generated** from the schema in `../schema`. Do not edit its
contents by hand — they are overwritten on each generation.

To (re)generate:

```bash
# from the workspace root
npm run sdk-gen
```
