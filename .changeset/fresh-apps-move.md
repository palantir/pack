---
"@palantir/pack.app.react": minor
"@palantir/pack.create-app": patch
"@palantir/pack.state.react": minor
---

Move the Pack app React context APIs from `@palantir/pack.state.react` into the new
`@palantir/pack.app.react` package. Update the workspace template to import the APIs from their new
package.
