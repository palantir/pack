---
"@palantir/pack.codegen.core": minor
"@palantir/pack.create-app": minor
---

Allow create-pack-app to use templates from local folders or installed npm packages. Templates can run a repository's package creator before adding files, skip the generator's npm install, and print their own setup instructions.
